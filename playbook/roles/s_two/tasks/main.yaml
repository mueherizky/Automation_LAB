#### Configure Trunk Link #### 
- name: "Configure Trunk S2"
  cisco.ios.ios_l2_interfaces:
    config:
      - name: GigabitEthernet0/3
        trunk:
          allowed_vlans: 2-3
          encapsulation: dot1q
          native_vlan: 1000
        mode: trunk

    state: merged